import React from 'react';
import Layout from '../../hoc/Layout';
import Quiz from '../../containers/Quiz';

const App: React.FC = () => {
  return (
    <Layout>
      <Quiz />
    </Layout>
  );
};

export default App;
